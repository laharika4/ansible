- name: removepackage 
  hosts: web
  become: yes
  tasks: 
    - name: remove few packages
      ansible.builtin.package:
        name: "{{ item.name }}"
        state:  "{{ item.state }}"
      loop:
       - { name: "git", state: "present" }
       - { name: "gcc" , state: "absent"}