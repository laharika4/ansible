- name: packageinstallation
  hosts: web
  become: yes
  tasks:
   - name: installing various packages 
     ansible.builtin.package:
        name: "{{ item }}"
        state: present
     loop:
        - mysql
        - gcc
        - nginx
        - zip
